/*
 * Full-page PDF facsimiles are intentionally disabled.
 *
 * The visible book must be the semantic HTML/CSS conversion. Genuine source
 * illustrations may remain images, but a screenshot of the complete printed
 * page must never hide or replace the converted content.
 *
 * This file remains as a harmless compatibility stub because existing page
 * shells already reference it.
 */
